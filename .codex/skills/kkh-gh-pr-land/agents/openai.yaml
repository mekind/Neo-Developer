interface:
  display_name: "KKH Land PR"
  short_description: "KKH document, push, land to main"
  default_prompt: "Use $kkh-gh-pr-land to document the completed work in repo docs, push the current branch, open a PR to main, resolve conflicts intentionally, and merge it safely into main."
